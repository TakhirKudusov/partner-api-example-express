import {TPartnerCreditResBody} from "../types";

export const credit: TPartnerCreditResBody = {
    Error: 0,
    Msg: "Ok!",
    Balance: 100,
    ExtSession: "external_session",
    ExtTransactionId: "external_transaction_id"
}